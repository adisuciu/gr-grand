# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-grand
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

include_directories(${Boost_INCLUDE_DIR})
link_directories(${Boost_LIBRARY_DIRS})
include_directories(${VOLK_INCLUDE_DIRS})
link_directories(${VOLK_LIBRARIES})

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX
list(APPEND grand_sources
    basic_logger.h
    sensor_base.cc
    accelerometer_impl.cc
    light_sensor_impl.cc
    opensl_sink_impl.cc
    opensl_source_impl.cc
    float_array_impl.cc
)

list(APPEND GRAND_LIBS
  android
  log
  OpenSLES
)

set(grand_sources "${grand_sources}" PARENT_SCOPE)
if(NOT grand_sources)
	MESSAGE(STATUS "No C++ sources... skipping lib/")
	return()
endif(NOT grand_sources)

add_library(gnuradio-grand SHARED ${grand_sources})
target_link_libraries(gnuradio-grand gnuradio::gnuradio-runtime ${GRAND_LIBS} Volk::volk)
target_include_directories(gnuradio-grand
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-grand PROPERTIES DEFINE_SYMBOL "gnuradio_grand_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-grand PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-grand)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_grand_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-grand)

if(NOT test_grand_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_grand_sources)

foreach(qa_file ${test_grand_sources})
    GR_ADD_CPP_TEST("grand_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
